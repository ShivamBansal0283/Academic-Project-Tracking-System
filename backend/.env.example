# Prisma
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"

# Auth (simple for now; you can switch to real auth later)
JWT_SECRET="supersecret"
PORT=4000
ORIGIN=http://localhost:5173
